"""Feature modules — vertical slices of business logic (users, auth, …).

Each sub-package exposes a public API via its own ``__init__.py``.
Cross-module access must go through those public surfaces only.
"""
